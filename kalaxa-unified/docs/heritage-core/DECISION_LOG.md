# دفتر ثبت تصمیم‌ها

| شناسه | تاریخ | تصمیم | دلیل | بازگشت‌پذیری |
|---|---|---|---|---|
| D-001 | 2026-07-17 | نام و فضای نام اختصاصی: ماژول Ruby سطح بالا `Kalaxa`، پوشهٔ افزونه `kalaxa/`، لودر `kalaxa.rb`، فضای نام AttributeDictionary برابر `kalaxa` | یکتا، کوتاه، بدون تداخل با افزونه‌های رایج؛ الزام «فضای نام اختصاصی» در پرامپت اصلی | بالا (تغییر نام قبل از مرحلهٔ 02 کم‌هزینه است) |
| D-002 | 2026-07-17 | واحد داخلی طول: میلی‌متر به‌صورت Integer (میکرون در موارد دقیق CNC: Integer برحسب 0.001mm فقط در لایهٔ Post) | صنعت کابینت ایران/جهان متریک است؛ Integer از خطای ممیز شناور در BOM و برش جلوگیری می‌کند؛ تبدیل به Length اسکچاپ فقط در Adapter | متوسط |
| D-003 | 2026-07-17 | ذخیره‌سازی دادهٔ مدل: JSON نسخه‌دار داخل AttributeDictionary با کلید `kalaxa:doc` + شمارهٔ `schema_version` | یک منبع حقیقت واحد، مهاجرت‌پذیر، مستقل از هندسه | بالا تا قبل از مرحلهٔ 02 |
| D-004 | 2026-07-17 | شناسهٔ پایدار موجودیت‌ها: UUID v4 تولیدشده در دامنه، ذخیره در JSON و آینه‌شده روی ComponentInstance | PersistentId اسکچاپ بین نسخه‌ها/کپی‌ها قابل اتکا نیست؛ UUID مستقل از هندسه است | بالا |
| D-005 | 2026-07-17 | رابط کاربری: HtmlDialog + TypeScript + Vite، خروجی باندل استاتیک بدون وابستگی زمان اجرا به اینترنت | الزام پرامپت اصلی + Offline-first | متوسط |
| D-006 | 2026-07-17 | حداقل نسخهٔ Ruby هدف: 3.2 (SketchUp 2024 به بعد) | فقط نسخه‌های هدف 2024..2026 پشتیبانی می‌شوند | بالا |
| D-007 | 2026-07-17 | تست: Minitest برای دامنهٔ Ruby خالص (اجرا خارج از اسکچاپ) + TestUp 2 برای تست داخل اسکچاپ + Vitest برای UI | جداسازی دامنه از API تست‌پذیری بدون اسکچاپ را ممکن می‌کند | بالا |
| D-008 | 2026-07-17 | زبان رابط کاربری نسخهٔ اولیه: فارسی (RTL) با زیرساخت i18n برای افزودن انگلیسی | بستهٔ نیازمندی فارسی است؛ i18n از ابتدا ارزان است و بعداً گران | بالا |
| D-009 | 2026-07-17 | جهت باز شدن مستندات پروژه: همهٔ مستندات مخزن فارسی، نام فایل‌ها و شناسه‌های کد انگلیسی | خوانایی برای کارفرما + سازگاری ابزارها | بالا |
| D-010 | 2026-07-17 | رابط مرحلهٔ ۰۱ با JavaScript ساده (ES5/6، بدون build) به‌جای TypeScript+Vite؛ قرارداد پل بدون تغییر | شبکهٔ محیط عامل بسته است و npm در دسترس نیست؛ مهاجرت بعدی به TS بدون تغییر قرارداد ممکن است (اصلاح D-005) | بالا |
| D-011 | 2026-07-17 | ذخیرهٔ تنظیمات/لاگ در %APPDATA%\Kalaxa به‌صورت JSON اتمیک (متغیر KALAXA_DATA_DIR برای تست) | تست‌پذیری خارج از اسکچاپ + استاندارد ویندوز | بالا |
| D-012 | 2026-07-17 | RBZ = زیپ استاندارد؛ سه مسیر ساخت هم‌ارز: build_rbz.rb (با Ruby)، build_rbz.ps1 (ویندوز بدون Ruby)، و ساخت مرجع این جلسه؛ چیدمان ریشه: kalaxa.rb + kalaxa/ | ساخت باید بدون وابستگی شکننده روی سیستم کارفرما ممکن باشد | بالا |
| D-013 | 2026-07-17 | راستی‌آزمایی مرحلهٔ ۰۱ دو-بخشی شد: بخش قابل‌اجرا در محیط عامل (JS/JSON/HTML/RBZ) واقعاً اجرا شد؛ تست‌های Ruby با وضعیت صادقانهٔ NOT_TESTED به کارفرما واگذار شد | مفسر Ruby در محیط عامل موجود نیست و نصب آن ناممکن بود (L-02) | — |
| D-014 | 2026-07-17 | دروازهٔ ۰۱ با دستور صریح کارفرما («ادامه بده 02 رو» پس از دو بار تذکر الزام عبارت تأیید) عبور داده شد؛ وضعیت ۰۱ = APPROVED با ریسک ثبت‌شده: تست‌های رینگ B/C هنوز توسط کارفرما اجرا نشده‌اند و هر ایراد آن‌ها به‌صورت CHANGES_REQUESTED روی ۰۱ برمی‌گردد | تصمیم متعلق به کارفرماست؛ قصد صریح و مکرر بود | — |
| D-015 | 2026-07-17 | موجودیت‌های دامنه هش‌های سادهٔ Ruby با سازندهٔ ماژولی (نه کلاس‌های سنگین) | رفت‌وبرگشت JSON بدون اتلاف + حداقل ریسک زمان اجرا در نبود مفسر برای تست فوری | متوسط |
| D-016 | 2026-07-17 | چک‌سام sha256 روی فرم متعارف (کلید مرتب، بدون فاصله، بدون Float)؛ پاکت‌های v1 بدون چک‌سام پذیرفته می‌شوند | تشخیص خرابی/دستکاری + سازگاری میراثی | بالا |
| D-017 | 2026-07-17 | راستی‌آزمایی دوپیاده‌سازی: مشخصات Canonical/Validator/Migration هم در Ruby و هم در Python مرجع (tools/verify_data_spec.py) پیاده و روی فیکسچرهای مشترک طلایی سنجیده می‌شود؛ نسخهٔ Python در همین جلسه واقعاً اجرا و PASS شد | دفاع در برابر خطای تک‌پیاده‌سازی در نبود Ruby در محیط عامل | — |
