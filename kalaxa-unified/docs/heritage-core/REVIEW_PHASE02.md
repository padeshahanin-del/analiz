# بازبینی مستقل فاز ۰۲ — کارفرما (با مشاور فنی)
تاریخ: 2026-07-19

## راستی‌آزمایی‌های واقعاً اجراشده در بازبینی (مستقل از عامل)
- `tools/verify_data_spec.py` دوباره اجرا شد: **۹/۹ PASS** (شامل بازتولید چک‌سام طلایی، لو رفتن دستکاری، استقلال چک‌سام از ترتیب کلیدها).
- اعتبار همه JSONها، سینتکس `app.js`، برابری ۱۸ کلید i18n دو زبان، نسخه 0.2.0 سازگار: PASS.
- تطابق محتوایی rbz منتشرشده با `src/`: یکسان (به‌جز build_info).

## یافته‌ها
1. **[Minor — رفع شد در همین بازبینی]** پوشه زبالهٔ `src/kalaxa/{app,ui` (باقیمانده brace expansion شکست‌خورده؛ خالی). در rbz فاز ۰۲ نبود ولی `cp_r` در build آن را وارد ساخت‌های بعدی می‌کرد.
   - اقدام: پوشه حذف شد؛ **گارد بهداشت بسته** به هر دو مسیر ساخت (`build_rbz.rb` و `build_rbz.ps1`) اضافه شد: فقط پوشه‌های مجاز `adapter/app/domain/i18n/persistence/ui` وارد RBZ می‌شوند و هر مورد غیرمنتظره با هشدار حذف می‌شود.
   - rbz با مسیر ساخت مرجع (مجاز طبق D-012) بازساخته شد؛ همه فایل‌ها بایت‌به‌بایت با نسخه عامل یکسان‌اند (فقط build_info زمان جدید دارد). testzip و چیدمان ریشه PASS.
2. **[نکته]** هم‌ارزی کامل fallback پاورشل با `zip -r` تضمین‌شده نیست؛ پس از اولین ساخت ویندوزی، چیدمان ریشه rbz یک‌بار چک شود (testzip در RELEASE_CHECKLIST هست).

## حکم
**تأیید مرحله: 02** — با اصلاح جزئی بالا که در همین بسته اعمال شده است.

## تعهدات باز کارفرما (پیش‌نیاز اتکای کامل، نه مانع ورود به ۰۳)
- اجرای `ruby test\unit\test_phase02.rb` (۱۵ تست) و `ruby test\unit\test_phase01.rb` (۱۸ تست) روی ویندوز.
- اجرای سوئیت‌های TestUp رینگ B هر دو فاز داخل SketchUp (ریسک ثبت‌شده D-014 هنوز باز است).
- نتایج به عامل و مشاور گزارش شود؛ هر شکست = CHANGES_REQUESTED روی فاز مربوط.

## یادداشت راهبردی برای فازهای بعد
مدل دادهٔ کالاکسا (parts با length/width/thickness_mm، grain، edgebanding، materials با sheet_*) با قرارداد `kitchen_snapshot` پروژهٔ آنالیز کالاکسا قابل نگاشت مستقیم است. برای فاز خروجی‌های تولید (نقشه برش/BOM)، ساخت یک آداپتور کوچک `doc → kitchen_snapshot v2` به‌جای بازنویسی، همهٔ ماژول‌های تست‌شدهٔ موجود (nesting گیوتینی + اعتبارسنج مستقل + نقشه برش/نصب + یراق — ۱۷۴ تست سبز) را مستقیماً در اختیار کالاکسا می‌گذارد.
