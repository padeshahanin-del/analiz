# هیچ تبدیل خودکار سطر انجام نشود.
#
# گیت روی ویندوز به‌طور پیش‌فرض LF را موقع checkout به CRLF تبدیل می‌کند.
# این‌جا نباید: پروژه بستهٔ RBZ می‌سازد و فایل‌های طلایی و چک‌سام دارد.
# اگر دستگاه دوم بایت‌های دیگری checkout کند، بسته‌ای می‌سازد که با بستهٔ
# دستگاه اول یکی نیست — و «چرا خروجی دو دستگاه فرق دارد؟» سخت‌ترین نوع
# باگ برای پیدا کردن است.
#
# `-text` یعنی: همان بایت‌هایی که commit شد، همان بایت‌ها checkout شود.
* -text

# دودویی‌ها، صریح
*.rbz binary
*.pdf binary
*.png binary
*.jpg binary
*.skp binary
