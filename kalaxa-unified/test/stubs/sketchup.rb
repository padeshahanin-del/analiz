# encoding: utf-8
# frozen_string_literal: true

# stub خالی برای `require 'sketchup.rb'`.
#
# چند فایل مرزی (raw_geometry، import_selection، create_*_tool، analysis_panel)
# این فایل را require می‌کنند. داخل اسکچاپ، خودِ برنامه فراهمش می‌کند؛ بیرون از آن
# وجود ندارد و require با LoadError می‌ترکد — به همین دلیل این فایل‌ها تا امروز
# اصلاً در هیچ تستی بارگذاری نمی‌شدند.
#
# این stub عمداً **هیچ‌چیز تعریف نمی‌کند**: هر تستی که به کلاس‌های اسکچاپ نیاز دارد،
# بدل خودش را می‌سازد. اگر این‌جا کلاس تعریف می‌شد، تست‌ها روی یک اسکچاپِ خیالیِ
# مشترک تکیه می‌کردند و واگرایی‌اش با API واقعی دیده نمی‌شد.
#
# استفاده:
#   $LOAD_PATH.unshift(File.expand_path('../stubs', __dir__))
